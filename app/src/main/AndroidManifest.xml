<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.incredible.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FF" />
        <activity android:name=".FS" />
        <activity android:name=".SF" />
        <activity android:name=".SS" />
        <activity android:name=".TF" />
        <activity android:name=".TS" />
        <activity android:name=".Frthf" />
        <activity android:name=".Frths"></activity>
    </application>

</manifest>